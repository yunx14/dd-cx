<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>{{title}}</title>
    <link href="{{stylesheet}}" rel="stylesheet"/>
  </head>
  <body>
    <input type='hidden' id='lat' name='lat' value='{{lat}}'/>
    <input type='hidden' id='long' name='long' value='{{long}}'/>
  {{> (lookup . "body") }}
  </body>
  {{#each scripts}}
  <script src="{{script}}" defer="defer"></script>
  {{/each}}
  <script>
  // if this doesn't work grab the above field values and pass to this function
    mapLocation({{lat}}, {{long}})</script>
</html>
