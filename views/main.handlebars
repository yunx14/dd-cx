<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>{{title}}</title>
    <link href="{{stylesheet}}" rel="stylesheet"/>
  </head>
  <body>
  {{> (lookup . "body") }}
  </body>
  {{#each scripts}}
  <script src="{{script}}" defer="defer"></script>
  {{/each}}
  <input id="latlonglat" type="hidden" name="locationLat" value="{{lat}}">
  <input id="latlonglong" type="hidden" name="locationLong" value="{{long}}">
  <script src="../public/template3.js" defer="defer">mapLocation($('#latlonglat').val(), $('#latlonglong').val())</script>
</html>
