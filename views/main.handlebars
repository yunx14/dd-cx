<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>{{title}}</title>
    <link href="../public/styles/style.css" rel="stylesheet"/>
  </head>
  <body>
  {{> (lookup . "body") }}
  </body>
  {{#each scripts}}
  <script src="{{script}}" defer="defer"></script>
  {{/each}}
</html>
